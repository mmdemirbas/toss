This directory contains vendored JS/CSS/font dependencies.

Run `make vendor` to re-download them (only needed to update lib versions).

These files are embedded into the Go binary for fully offline operation.
